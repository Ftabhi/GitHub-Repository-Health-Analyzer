"""Data cleaning helpers."""
import pandas as pd


def clean_commits(df: pd.DataFrame) -> pd.DataFrame:
    """Perform basic cleaning on commits DataFrame."""
    df = df.copy()
    # placeholder cleaning steps
    return df
